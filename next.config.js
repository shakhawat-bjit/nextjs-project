/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    // domains: ["c0.wallpaperflare.com"],
    // remotePatterns: [
    //   {
    //     protocol: "https",
    //     hostname: "**",
    //   },
    // ],
  },
  output: "export",
};

module.exports = nextConfig;
